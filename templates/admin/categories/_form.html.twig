{{ form_start(catForm) }}
    {{ form_row(catForm.name) }}
    {{ form_row(catForm.parent) }}
    {{ form_row(catForm.categoryOrder) }}

    <button type="submit", class="btn btn-primary">{{ button_label|default('Ajouter')  }}</button>
{{ form_end(catForm) }}